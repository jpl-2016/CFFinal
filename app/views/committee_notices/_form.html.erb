<%= form_for(@committee_notice) do |f| %>
  <% if @committee_notice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@committee_notice.errors.count, "error") %> prohibited this committee_notice from being saved:</h2>

      <ul>
      <% @committee_notice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :committee_id %><br>
    <%= f.number_field :committee_id %>
  </div>
  <div class="field">
    <%= f.label :campaign_finance_info_id %><br>
    <%= f.number_field :campaign_finance_info_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
