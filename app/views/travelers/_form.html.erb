<%= form_for(@traveler) do |f| %>
  <% if @traveler.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@traveler.errors.count, "error") %> prohibited this traveler from being saved:</h2>

      <ul>
      <% @traveler.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :travel_id %><br>
    <%= f.number_field :travel_id %>
  </div>
  <div class="field">
    <%= f.label :entity_id %><br>
    <%= f.number_field :entity_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
