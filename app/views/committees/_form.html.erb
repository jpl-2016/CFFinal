<%= form_for(@committee) do |f| %>
  <% if @committee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@committee.errors.count, "error") %> prohibited this committee from being saved:</h2>

      <ul>
      <% @committee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :treasurer_id %><br>
    <%= f.number_field :treasurer_id %>
  </div>
  <div class="field">
    <%= f.label :committee_type_id %><br>
    <%= f.collection_select :committee_type_id, CommitteeType.all, :id, :committeetype %>
  </div>
  <div class="field">
    <%= f.label :address_id %><br>
    <%= f.number_field :address_id %>
  </div>
  <div class="field">
    <%= f.label :committeename %><br>
    <%= f.text_field :committeename %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
